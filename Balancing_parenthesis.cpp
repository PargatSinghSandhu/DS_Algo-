//
//  Balancing_Parenthesis.cpp
//  DSA
//
//  Created by Pargat Singh on 4/10/23.
//

#include <stdio.h>



for (int i=0;i<n;i++)
{
    if(exp[i]=='{' or '[' or '(')
        push(exp[i]);
    else if exp[i] is ')' or '}' or ']'
    {
        if(S is empty) || (top )
    }
}
